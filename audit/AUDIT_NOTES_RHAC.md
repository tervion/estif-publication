# AUDIT NOTES — RHAC.md (main-documents pass, doc 6 of 7)
**Audit date:** 18 July 2026 · **Scope:** `docs/plan/RHAC.md` (repo copy, stamped 6.4.1 / 13 Jul, header and footer) vs the receipt corpus, the four session-folder passes, the STATUS/README/CHANGELOG/VALIDATION/CONCEPT passes, and `DOC_UPDATE_PLAN_v6.4.0.md` §A (verbatim diff of the planned vs landed RHAC-007/008/009 texts and the four header/footer/status edits) · **Method:** full read of the entire archive — historical scenarios A–M and 1–8, the v6.1/v6.3 update layers, the axiom amendment, RHAC-001…010, both statistics tables — with every cited receipt filename checked against the live `tests/` and `docs/plan/` listings; entry numbers cross-checked against runtime results from the session passes. Per instruction, `CITATION.cff`, `SUMMARY_FOR_REVIEW.md`, and `docs/Letter/` were not opened.

**Overall verdict: THE STAMP IS HONEST AND THE PLAN LANDED VERBATIM — THE DEBT IS THE ALREADY-KNOWN WIRING PLUS A FORWARD-CHRONOLOGY DISEASE THE FILE'S OWN CONVENTION ALREADY CURES.** RHAC-010 is present in full (the third honest 6.4.1 stamp, joining README and CHANGELOG against the false-stamp set STATUS/VALIDATION/CONCEPT), DOC_UPDATE_PLAN §A executed content-identically, and Scenario H and Q carry their RHAC-009 closures exactly as scheduled — H/Q's closure language is the fix model for VALIDATION V-7. The confirmed debts: the Jul 12 receipt-wiring findings on RHAC-004/005/006 stand unapplied (the archive layer of the now-seven-surface batch); and because this file reads **forward**-chronologically — the opposite of CHANGELOG's self-healing reverse order — stale early statuses mislead unless they carry the "see update below" pointer the file already uses for Scenarios E and I. Scenario H's original still reads 🟡 Active with "the most important result in the project" framing; Scenario O still reads 🟡 Active though the CONCEPT pass verified its writing task complete; Scenario Q still demands a Gaztañaga memo "prerequisite" that has since been delivered with a no-novelty verdict.

Resolution of the CONCEPT-pass carry-forward questions for this document: RHAC-007/008/009 vs plan §A — **content-identical; whitespace-only drift on wrapped lines in 008/009; the 008 NOTE is an honest post-plan addition (13 Jul)** (RH-9). Scenario H closure on H itself — **executed verbatim on the re-update; the *original* H block upstream has no forward pointer** (RH-2). Scenario Q wording vs VALIDATION §3.0 — **matches; Q is §3.0's source and they agree to the digit**. 008 stale note — **confirmed, plus its twin caveat in the header status line; both clear with E-2** (RH-7). ~1e11 qualifier — **still missing in RHAC-004** (rides the batch). Header/footer stamps — **6.4.1 / 13 Jul, and honest: RHAC-010 appended in full**. Summary-statistics recount — **never done; the v6.4.1 table carries an honest do-not-cite caveat instead, and the recount is genuinely ambiguous** (RH-8).

One clarification of the Jul 9 record: AUDIT_NOTES_JUL9 §B listed "Scenario H (re-update)" as absent from the repo. The repo **does** contain an H re-update (routes + closed status, confirmed this pass); what never landed is the *v6.3-append intermediate version* of that section — the trail's middle step, exactly as the Jul 9 note's nuance stated. No contradiction; the optional backfill remains optional.

---

## A. Findings (ordered by severity)

### RH-1 — MEDIUM-HIGH (pre-specified; confirmed unapplied) · The Jul 12 receipt-wiring findings stand — the archive layer of the seven-surface batch
Re-verified in place:
- **RHAC-004** still cites only `tests/test_UKN.py` (the door-3 swirl draft) for the full four-door census; door 2's pure-divergence theorem — the receipt carrying the "slosh ZERO" verdict the entry itself states — remains uncited, as does door 1. The "~1e11 below rho_L" clause still lacks the "(order-of-magnitude)" qualifier (CL-7 twin).
- **RHAC-005** still carries the wrong-file citation outright: "Receipts: tests/test_UKN2.py (prints [0,0,0], 0, 0, 0)" — that output signature belongs to `estif_growth_nogo_audit.py`; UKN2 prints a single `0` and contains only the A1′ deepen proof; the entry's own −0.91 is printed by `estif_growth_nogo_law.py`, uncited.
- **RHAC-006** still cites UKN2 (right content, wrong canonical name) and still states "PHASE-2 RE-AUDIT under A1′: five sectors, no Lambda-printer — null stands. Receipts: tests/test_UKN2.py" — the claim with **no machine receipt anywhere in the repo**. The standing decision (write the ~20-line A1′ re-census receipt vs reword to pencil-level) now covers RHAC-006, PHASE2_DECLARATION, and CHANGELOG CL-5.
Nothing new here — the fixes are fully specified in the Jul 12 pass. This confirms the batch is ready to execute across **seven surfaces in one commit**: RHAC-004/005/006, PHASE2_DECLARATION, TEST_INDEX, STATUS test table, CHANGELOG [6.4.0], CONCEPT Reframe line.

### RH-2 — MEDIUM-HIGH · Scenario H's original block reads 🟡 Active with no forward pointer — while its re-update reads CLOSED
The file is forward-chronological. Scenarios E and I originals carry the file's own cure — "see Scenario E update below" / "see Scenario I update below" — but Scenario H's original ends at "**Current status:** 🟡 Active — open theoretical question … If it gives exactly x₀ × ρ_crit, this becomes **the most important result in the project**," with no pointer to the re-update ~300 lines down whose status is 🔴 CLOSED (RHAC-009). A reader working forward takes the project's closed top target as its live one. Scenario D's original has the same gap (its re-update exists below, unpointed). **Fix:** apply the file's own convention — one "see re-update below (CLOSED via RHAC-009)" line on H, one "see re-update below (moot under the reframe)" line on D.

### RH-3 — MEDIUM · Scenario O still reads 🟡 Active — the task it created is verified complete
"**Status:** 🟡 Active (Path One writing task)." The CONCEPT pass verified all three items executed: A2/A3 written into ESTIF_CONCEPT.md, the shrinking-ruler narrative retired, 0.31c relabelled as the sideways component. The entry is also internally inconsistent (✅ in its title, 🟡 in its status). This is the RHAC-side twin of STATUS S-7 — the archive entry that *created* the writing task is the last surface still showing it open. **Fix:** one dated closure line ("🟢 Resolved — executed in v6.3.1; verified against ESTIF_CONCEPT.md 18 Jul 2026").

### RH-4 — MEDIUM · Scenario Q's flag 2 demands a prerequisite that has since been satisfied — and the verdict is recorded everywhere except here
Flag 2: "Gaztañaga adjacency… A comparison memo is a **prerequisite for any novelty claim** — see checklist B-4b. **Until it exists, no priority is asserted.**" The memo exists (`docs/plan/GAZTANAGA_COMPARISON.md`), delivered with the 🔴 no-novelty verdict, recorded in VALIDATION §3.0 flag 2, README, and SUMMARY (per the Jul 13 matrix) — but not in the entry that demanded it. Q's Decision line has the same residue: "Do not upgrade any Ωm language … until *both* Part B lands and the Gaztañaga memo clears" — both conditions have now resolved (both negatively: Part B closed non-derivable; memo returned no priority), which means the C2 language is **permanent**, and only the Part-B half is recorded in the status line. **Fix:** one dated annotation on flag 2 ("memo delivered — verdict: no ESTIF priority on Ωm; cite Gaztañaga prominently; a₀ = cH₀/2 link is the distinctive thread") noting the decision's conditions are fully resolved.

### RH-5 — MEDIUM · Stale-status cluster in the historical and RHAC layers (forward-chronology disease, smaller instances)
1. **Scenario G** — "🟡 Active — Tully-Fisher test pending, then simulation wall." The TF test resolved in v6.1 (Scenario I update: zero M-dependent factor; MOND limit gives 1/4). Only the wall remains. One status line.
2. **Scenario J** — "🟡 Active — Phase 6.1 — blocked by cosmology sector failure … must fix Ω_tilt(z) first." Moot under the reframe: on Path One (pure Λ) the CMB check is standard ΛCDM (VALIDATION Part 7 states this). Needs the same treatment Scenario D's re-update got ("Superseded/moot"). Note: J's proposed `test_cmb_angle_estimate.py` was never written — an option-proposal, not a false receipt claim, but worth naming in the supersession line.
3. **RHAC-003** — the far-future block's "the operational handle is w(a) = Path Two" and "First battle already scheduled and pre-registered" are future-tense for events that have occurred: the battle fired the NULL branch (RHAC-004, the very next entry). One post-hoc line ("battle fought — RHAC-004: w = −1 exactly; forecast condition (a) currently holds").
4. **RHAC-001** — road R3 ("vorticity T_μν → field equation … μ and the Path Two thawing term come from the same object — TAKEN") is half-dead: the object was computed (door 3) and the thawing half returned the null (RHAC-004); the μ half remains open (`mu_extraction.py`; letter limitation (ii)). A reader following the TAKEN road today walks into the null unpointed. One post-hoc line.
5. **RHAC-002** — "post-Path-Two at the earliest" timing anchor for the G1 gate is now a passed anchor (Path Two concluded, as a null). Optional one-word touch; the gate itself is unaffected.
All five use the file's existing mechanism: annotate with a date, never rewrite.

### RH-6 — MEDIUM · Header "Gravity letter ready" — fifth live letter surface
The header status line joins STATUS S-4, VALIDATION V-4, and CONCEPT C-5.4 (README R-2 inverts it). The frozen instances — Scenario K ("ready for submission," v6.1), Scenario M ("letter ready," v6.2), the v6.3 milestone block — were true when written and are tolerable as history; the **live header** is the fix surface. Rides the single gated formulation: five live surfaces, one wording, one commit. (For the record: Scenario M's Blocker-2 resolution language — "physically motivated, not fitted" — is *closer to RHAC-010-compliant* than CONCEPT's "forced, not chosen"; no action needed on M.)

### RH-7 — LOW · The stale-in-the-good-direction pair clears with E-2
The 13 Jul NOTE inside RHAC-008 ("receipt filename … not found in the repository … Locate and commit, or amend") and its twin caveat in the header status line ("RHAC-007/008 — receipt filenames unverified, see note below") — both were the log doing its job on 13 Jul; both are outdated now (15 Jul reconstructions committed; cross-validated 17 Jul against the surviving originals in `audit/Jul 13 Session/`; all shared numbers agree). Both clear in the E-2 batch with CL-3, the two TEST_INDEX ❌ rows, and `_index_view/MISSING.txt` — **gated on the Mac mini run**. Replacement text records the resolution, per the CL-3 model.

### RH-8 — LOW / DECISION · Two statistics tables; the recount the plan flagged remains undone — and is genuinely ambiguous
The v6.1-era "Updated Summary Statistics" (21 scenarios) sits mid-file with **no supersession marker**; the v6.4.1 table (25 scenarios) sits at the bottom with an honest do-not-cite caveat ("counts predate RHAC-007–010 and Scenario H's closure — recount before citing"). The 25-total verifies for the scenario layer (A–M = 13, 1–8 = 8, N/O/P/Q = 4). The recount is ambiguous because no rule says whether RHAC-001…010 count as "scenarios" (if yes, total 35; if no, the delta is H's closure: Active 6→5, Resolved 16→17 — plus O's closure if RH-3 lands). **Decision:** recount with an explicit counting rule, or keep the caveat and add a one-line "superseded by the v6.4.1 table below" marker on the old table. Either is defensible; the current caveat is honest.

### RH-9 — LOW · Plan-vs-landed diff results (the check this pass was asked to run)
RHAC-007 and RHAC-009 landed **content-identical** to DOC_UPDATE_PLAN §A; RHAC-008 likewise, with whitespace-only drift (wrapped continuation lines lost their two-space indent in 008's A1′-HINGE block and 008/009's closing lines) — cosmetic, no action. All four scheduled header/footer/status edits executed (header→008-note caveat added on top on 13 Jul; footer restamped 6.4.1/13 Jul with RHAC-010 — content matches). The r_p/d_p drift inside RHAC-009 (QUESTION line r_p, RESULT line d_p) **landed verbatim from the plan** — this entry is the origin point of the corpus-wide notation drift (V-9/CL-8/R-10.1); the unification decision should be recorded here when taken. Optional cross-refs from the Jul 12 pass remain available: RHAC-006 "(derived: RHAC-008)" on the GW anticipation; RHAC-009's redirect line "(executed: RHAC-010)" — the redirect *was* followed and produced 010 the next day; the sequence is coherent, the pointer just makes it legible.

### RH-10 — LOW · Cosmetics / dispositions
1. **"#15071442 - Unique ID STAMP"** — trailing artifact after the footer. Same family as `requirements.txt`'s APPROVED-FORK marker (R-4): flagged for disposition, **not corrected**, per the intentional-marker rule.
2. **Frozen "regime" instances** — RHAC-001 ("per-regime", "Regimes") is the entry that *coins* the gradient replacement ("gradient dial") — ruled tolerable as the coining record; Scenario C's "curvature regimes" is frozen v4.0 text. Recorded so the sweep doesn't false-positive.
3. **Frozen "frozen eddy"** — Scenarios E-re-update/P and the v6.3 milestone block use the term pre-retraction; RHAC-004 below retracts it explicitly in the same file. Ruled tolerable (the CL "flat-slice flow picture" precedent: accurate when written, superseding entry in the same document). Same ruling for Scenario N's "A1 flat slices" (the AXIOM AMENDMENT block supersedes it) and OLD-A1's "exactly flat" (quoting the retired axiom is compliant by construction).
4. **Scenario A's playbook** — "Point to LISA … as definitive tests" predates C1; optional clause if the playbooks are ever refreshed.

---

## B. What RHAC.md gets right (for the record)

**The third honest stamp.** 6.4.1 / 13 Jul on both header and footer, and the content is there: **RHAC-010 appended in full** — doctrine statement, the algebraic equivalence (c²√Λ = √3·√Ω_Λ·cH₀), the retirement verbs on "√3 derived" language, the honest STILL-OPEN prefactor clause (cH vs cH/2π vs k≈0.128), the x_c double-duty flag with a named document and section (`ESTIF_document_update_guide.md` §6 — the source CHANGELOG's resolution of README R-10.2 traced to), all four receipts (exist in `tests/`, verified), and the exactly-right scoping clause ("does not touch the Ωm bootstrap … or Part B's closure — those concern Ωm, not a₀'s own derivation status"). The honest-stamp set is now {README, CHANGELOG, RHAC}; the false-stamp set {STATUS, VALIDATION, CONCEPT}; ROADMAP pending.

**The plan landed.** All of DOC_UPDATE_PLAN §A verified executed: three entries content-identical, four FIND/REPLACE edits in place, Scenario H's re-update status and Scenario Q's Part-B closure **verbatim as scheduled** — and Q's closure wording agrees with VALIDATION §3.0's citation of it to the letter. **H/Q's closure language is the fix model for VALIDATION V-7** (§3.1's stale "open task (RHAC Scenario H)" pointer should copy it).

**Scenario Q's Part A is receipt-faithful to the digit**: 0.3043 / 0.31408 / 0.96% / 0.53σ / 4.353×10²⁶ m / −1.07% / 1.1920×10⁻¹⁰ / 1.72%→0.66% / ×1.00269 / 1.618 vs 1.919 / input ledger {H₀, T_CMB, N_eff} — all match §3.0 and the receipts. The four honest flags are intact, and flag 2's discipline ("no priority is asserted" until the memo exists) is exactly why RH-4 is an annotation, not a correction — the entry enforced the standard; the record just hasn't caught up to its satisfaction.

**RHAC-007's four-lock ledger carries the honest C-11 clause** ("none separates ESTIF-Core from FLAT ΛCDM at linear order") — the source of CHANGELOG's model version. Entry numbers match the session runtimes: f(0.5)=0.7603, fσ8 −0.12/+1.03, g(9.1)=0.998 (receipt prints 0.9978), γ 0.5455–0.5544, −0.23σ, 28/212 Myr, 10⁴–10⁶ M☉, ζ=10⁻⁵ passport.

**The 13 Jul NOTE and header caveat were the log doing its job** — the archive flagged its own unverified citations the day it was stamped, which is why the E-2 resolution can be recorded rather than papered over.

**The forward-pointer convention already exists** (Scenarios E and I) — every RH-2/RH-5 fix is an application of the file's own pattern, not a new mechanism. Likewise the annotate-never-rewrite mechanism (RHAC-004's retraction paragraph even preserves the author's pre-derivation objection — provenance discipline at its best).

**Every receipt filename cited anywhere in the archive exists** in `tests/` or `docs/plan/` (checked against live listings): mu_extraction, UKN/UKN2, DECLARATION, front1/2/3, C15, P_derivation, the a₀-horizon four, the task-series, bootstrap pair, tully_fisher_correction, the update guide. The single ghost — Scenario J's `test_cmb_angle_estimate.py` — is a never-executed option-proposal inside a superseded scenario, not a receipt claim; no archive statement is false on receipts beyond the wiring already catalogued.

---

## C. Proposed actions (RHAC.md scope) — for your decision, nothing executed

1. **RH-1:** execute the seven-surface UKN batch as specified (Jul 12 fixes for 004/005/006 + "(order-of-magnitude)" on the 1e11 clause), and take the standing **A1′ re-census decision** (write the ~20-line receipt — recommended — or reword the three "re-audit" citations to pencil-level: 006, DECLARATION, CHANGELOG).
2. **RH-2…RH-5:** one dated annotation pass, file's own mechanism — H and D forward pointers; O closure; Q memo-delivered note; G status refresh; J supersession; 003 battle-fought line; 001 R3 half-closure line; optional 002 anchor touch.
3. **RH-6:** header letter line onto the single gated formulation (five live surfaces, one wording: STATUS, VALIDATION, CONCEPT, RHAC header + README's inversion).
4. **RH-7:** E-2 batch — replace the 008 NOTE and the header caveat with the resolution text (reconstruction 15 Jul; original preserved byte-exact in `audit/Jul 13 Session/`; cross-validated 17 Jul) — **after the Mac mini run is green**.
5. **RH-8:** stats decision — recount with an explicit rule (are RHAC-### entries scenarios?), or keep the caveat and mark the old table superseded.
6. **RH-9/RH-10:** optional cross-refs (006→008; 009→010); record the r_p/d_p canonical-form decision in 009 when the notation batch lands; stamp disposition on #15071442 (yours).

Per the single-pass preference, all of the above folds into the consolidated execution round from AUDIT_NOTES_JUL13 §F. The annotation pass (item 2) is a natural single commit distinct from the wiring batch (item 1), since one touches citations and the other statuses — or both ride the consolidated round together; your call on granularity.

---

## D. Carry-forward for the final document

- **ROADMAP.md (doc 7):** verify DOC_UPDATE_PLAN §G executed (the superseded-history banner + 6.4.0 stamps) — and whether a later hand restamped 6.4.1 (the family pattern predicts it; if so, check for RHAC-010 content or an honest banner extension). Verify coexistence with the Jul 9 ROADMAP_v6.3_APPEND (6/6 headers integrated) — the banner declares the body "largely superseded" history; confirm the append content sits under that declaration coherently. Check: letter-gating language vs the single formulation (the §G REPLACE text itself says "Gravity letter ready" — if landed verbatim, ROADMAP is the **sixth** live letter surface); every "derive Principle P" item in the preserved body sits under the banner's correction; any Path Two item still reading live (the V-3 disease in roadmap form); post-v6.4 items (Fronts 1–3 follow-ups, the RHAC-010 redirect of the old √3/x_c items — the banner's redirect line predates RHAC-010 by a day, so its "√3/cH₀ scale" wording may itself need the 010 reframe); whether ROADMAP maintains any parallel test/status table (S-9 disease). This is the last document; on completion, §F of the Jul 13 notes plus the seven doc-pass action lists constitute the full consolidated execution round — a final one-page batch summary may be worth producing as the round's checklist.

**Notes version:** 1.0 · 18 July 2026
