# AUDIT NOTES — ROADMAP.md (main-documents pass, doc 7 of 7)
**Audit date:** 19 July 2026 · **Scope:** `docs/guide/ROADMAP.md` (repo copy; header "Version 6.4.1 / 12 July 2026 / Status: v6.4.0 — …"; footer "6.4.1 / 12 July 2026") vs the receipt corpus, RHAC record, the four session-folder passes, and the STATUS/README/CHANGELOG/VALIDATION/ESTIF_CONCEPT passes · **Method:** full read + claim-by-claim cross-check on Claude's machine; `DOC_UPDATE_PLAN_v6.4.0.md` §G diffed **verbatim** against the landed header/banner/footer (plan read from `audit/Jul 13 Session/`); every script the document cites as existing/complete checked against the live `tests/` listing; all document pointers verified against this and prior passes' live listings. Per instruction, `CITATION.cff`, `SUMMARY_FOR_REVIEW.md`, and `docs/Letter/` were not opened. Carry-forward questions taken from AUDIT_NOTES_ESTIF_CONCEPT §D (the last notes file on hand); no `AUDIT_NOTES_RHAC.md` exists in `audit/` — RHAC cross-references below rely on the Jul 12/13 session passes and DOC_UPDATE_PLAN §A, not on a document-pass read of RHAC.md.

**Overall verdict: THE BEST SUPERSEDED-HISTORY MECHANISM IN THE CORPUS — WRAPPED AROUND THE ONLY DOCUMENT WHERE PATH TWO STILL READS LIVE.** The two-layer banner architecture is the model of its kind: the v6.3 banner explicitly converts the old EHT/Λ/LISA ✅ marks to C1-conditional, retires the Ω_tilt phases and the w = −1.08 prediction by name, and delegates authority to the checklist and RHAC with "where the two disagree, the checklist wins"; plan §G is confirmed verbatim-executed. The debt: the Version field was later bumped to 6.4.1 (header **and** footer — a restamp the Jul 13 execution matrix never recorded) with no date change and zero RHAC-010 content, leaving "Version 6.4.1" sitting two lines above a Status field that still opens "v6.4.0 —"; the live v6.3 tail carries a full PATH TWO research program (progress bar at 10%, P2.1/P2.2, priority row 5) with no closure marker anywhere in the body — the largest live-reading Path Two surface in any audited document, against one-clause residues elsewhere; and the letter overclaim appears on three surfaces, making ROADMAP the fifth document in that contradiction.

Resolution of the ESTIF_CONCEPT-pass carry-forward questions for this document: plan §G executed — **yes, verbatim; it coexists cleanly with the Jul 9 append (append = the v6.3 tail, 6/6 headers; §G = header + banner + footer)**. Later 6.4.1 restamp — **confirmed, header and footer; the family pattern held, and the restamp is absent from the Jul 13 matrix's "4 footers/headers" list — ROADMAP's two are the unrecorded 5th and 6th** (RM-1). Letter-gating language — **fails; three surfaces** (RM-3). "Derive Principle P" items under the banner's correction — **covered in spirit, unmapped in fact: the banner names "P"; the body's instances (Phase 7.3, Next Steps 5, the Mac-mini table row) never use the word** (RM-4). Post-v6.4 items — **absent; forward-looking content ends 8 July** (RM-9). Path Two reading live — **confirmed, three surfaces** (RM-2).

---

## A. Findings (ordered by severity)

### RM-1 — HIGH · Fourth member of the wrong-stamp family — and the only one that contradicts itself inside the header
Header: "**Version:** 6.4.1 / **Last Updated:** 12 July 2026 / **Status:** **v6.4.0** — A1→A1′; Phase 2 null; Fronts 1–3; C-15 … DERIVED; …". Footer: "6.4.1 / 12 July 2026". Diff against plan §G proves the mechanism: §G set **6.4.0 / 12 Jul** with exactly this Status text and banner (verbatim match, unicode included); a later hand bumped only the two Version fields to 6.4.1, leaving date and content behind — so the version number and the Status line's own label now disagree in adjacent lines. Same signature as STATUS S-1 and CONCEPT C-1 (6.4.1 number, 12 Jul date, zero RHAC-010 content), sharpened by the internal contradiction. Matrix correction: AUDIT_NOTES_JUL13 §B counted 4 restamped headers/footers (RHAC ×2, STATUS, VALIDATION); ROADMAP's header + footer make it 6.

What the missing RHAC-010 content costs here specifically:
- Banner pointer reads "`docs/plan/RHAC.md` (RHAC-001…**009**)" — stale; RHAC-010 was appended 13 Jul (Jul 13 matrix).
- The banner's closing redirect — "Redirect foundational effort to x_c = 0.272 or the √3/cH₀ scale" — is RHAC-009's 12-Jul phrasing of exactly the problem RHAC-010 restructured the next day (target-band / numerology-floor / river-edge; √3 prefactor demoted to working form).
- The preserved body's pre-demotion framings then stand uncorrected by any banner: Phase 7.0 "Four-step derivation of a₀ = H₀cx₀/√3 … **Zero free parameters**", 7.0c "x_c = 0.272: OPEN — the remaining theoretical gap", the whole of 7.0d, v6.1 next-steps row 4, and the v6.1 letter checklist's "1/3 derived from isotropy (same principle as 1/√3 in MOND)". The banner corrects the P items by name (RHAC-009) but never extends the same courtesy to the √3/x_c items (RHAC-010) — an asymmetry created purely by the banner's 12-Jul date.

**Fix (joint decision with S-1, V-1/V-2, C-1 — four documents, one branch):** content branch — extend the banner pointer to 001…010, add one RHAC-010 clause covering the √3/x_c items, take the RM-9 content decision, and stamp honestly; or restamp branch — revert both Version fields to 6.4.0 (two characters; date and Status are already honest at 6.4.0).

### RM-2 — HIGH · Path Two reads live across the entire live tail — the largest un-closed surface in any audited document
Three live surfaces, zero closure markers:
1. v6.3 progress bars: "Cosmology (Path Two = thawing eddy): █░░░ **10% 🔬 hard, vorticity T_μν**" — an in-progress bar for a branch closed 11 Jul.
2. "**PATH TWO — ESTIF-Extended (hard) 🔬 high-risk research**" with P2.1 (derive the vorticity stress tensor, "Target the mild DESI thawing (nominal χ²/N ≈ 0.66 …)") and P2.2 (CMB/ISW after P2.1) — presented as the standing plan.
3. Priority table row 5: "Vorticity stress-tensor derivation of leading w(z) | **Two** | Theory | unknown (hard)".

The honorable null (11 Jul, RHAC-004: residual sector empty, swirl ledger negative, w = −1 exactly under the pre-registered pass/fail) closed precisely this program — the vorticity-Tμν derivation P2.1 describes is the thing the three-door census proved empty. The document's only marker is the compressed "Phase 2 null" in the header Status; the banner never restates it, and a reader who follows the file's own instruction ("read the v6.3 update at the bottom first") lands directly on the un-closed fork. Comparison across the corpus: STATUS closed it correctly, CONCEPT closed all three body surfaces, README and VALIDATION each had one-clause residues (R-7, V-3) — ROADMAP alone carries a full live section. This is V-3's disease in roadmap form, as the CONCEPT pass predicted, at maximum severity.

**Fix:** closure markers on all three surfaces in the document's own idiom — bar → "CLOSED 11 Jul 2026 (honorable null, RHAC-004; w = −1 exactly)"; PATH TWO section header + one closure line citing RHAC-004 and `estif_pathtwo_target_lock.py` (the NULL branch that fired), keeping P2.1/P2.2 as the historical plan the null was tested against; row 5 struck with the same citation. Note what's already right inside P2.1: the C4 caveat ("⚠️ under-marginalized, see C4; re-derive with rd, H₀, Ωm marginalized before committing") is present and honest — that text rides the Jul 9 C4 commit (S-10 family) whenever it lands.

### RM-3 — MEDIUM-HIGH · Letter overclaim on three surfaces — fifth document in the contradiction
Header Status: "**Gravity letter ready.**" P1.4: "Submit the gravity letter (strengthened by the derived field equation)." Priority row 3: "Submit gravity letter (derived field equation + C1/C6 wording) | after 1–2." All three predate the Jul 11 pass's gate: the sendable letter is v6.3, which violates C1 on the LISA 491 µs line (B-1, filed pre-submission-critical), with the stale limitation (v), the unattributed a₀ reference, and the 1.92/1.965 ambiguity behind it. The corpus now holds four positions across five documents (STATUS "ready", README "blocked", VALIDATION header "ready", CONCEPT "deliverable today", ROADMAP "ready" ×3). **Fix:** the single agreed formulation on all three surfaces — "v6.3 exists; submission gated on the 6.4 revision (C1 LISA conditional; honorable-null limitation rewrite)". Historical note for fairness: "Gravity letter ready" was §G's own planned text — honest on 12 Jul, gated on 17 Jul; the fix is forward-looking, not a fault of the plan's execution.

### RM-4 — MEDIUM · The banner's P correction names "P"; the body's P items never do — and one of them is only partly closed
Banner: "'derive Principle P' — listed below and elsewhere as a top target — is CLOSED (RHAC-009)." The actual body instances are worded as the Ωm-derivation ambition, with the word "P" absent: Phase 7.3 ("**the most important remaining theoretical problem in the project** … If it succeeds, ESTIF derives Ωm from first principles"), Immediate Next Steps item 5 ("If ρ_eddy = x₀ × ρ_crit can be derived, Ωm is no longer a free parameter"), and the Mac-mini table row "Stress-energy tensor derivation | Theory | 7.3". A reader cannot map the correction onto its targets. Precision matters in the fix, because the corpus's own sanctioned wording (plan §D, landed verbatim in CONCEPT) splits the status three ways: **(a)** P as a derivable law — closed (RHAC-009, crossing-once obstruction); **(b)** the "Ωm derived from the axioms" ambition — retired; **(c)** whether ρ_eddy = x₀ρ_crit emerges from the homogeneous field equation — "a separate, still-open question." Phase 7.3 is route (c) framed with ambition (b)'s payoff. **Fix:** one banner clause naming Phase 7.3 / Next Steps 5 / the table row and applying the CONCEPT three-way wording — the derivation-of-Ωm payoff is retired; the homogeneous-emergence question survives as open, without the "most important remaining problem" billing.

### RM-5 — MEDIUM · Retracted-term and attribution cluster in live text
- "**frozen-eddy limit**" (fork paragraph: "Task 6 then showed the frozen-eddy limit (w = −1) ties ΛCDM…"), "**Path One = frozen eddy**" (progress bar), "**constant cosmic eddy**" (P1.3) — three live instances of the RHAC-004-retracted term (S-6/R-9/V-8/C-6 family; largest live-text count after VALIDATION). Sanctioned wording: "constant cosmic term (imported Λ)"; TEST_INDEX's task6 row is the model.
- Progress bar "Cosmology (Path One …): 100% ✅ ties ΛCDM, **derived**" — the corollary (constant ρ → de Sitter) is derived; the value is imported (V-8/C-6 verb nuance). "imported Λ" closes it.
- Bare **1.92** three times (v6.3 banner list "3.35 vs 1.92", the bar's "ties ΛCDM", P1.3) with no pipeline attribution — task6 constant-Λ-limit figure; Path One DR2 13-bin prints 1.965 (S-8/R-8 family; CHANGELOG [6.3.0] is the attribution model). One parenthetical at first use.

### RM-6 — MEDIUM-LOW · The errata item is stale in both its slots
P1.2b: "Apply the v6.3.1 errata (C1–C6) across the repo. ⬜ This is now the gating item before submission." Priority row 1: same, "days." Current truth: **doc-side C1–C6 applied** (v6.3.2 → 6.4.1 passes confirmed it document by document); **script-side C2/C3/C4/C5 pending** — with C3 in the list (the committed `estif_jwst_growth_spec.py` re-verified pre-C3 on 17 Jul). ROADMAP escapes the C3-omission disease only because it never enumerates; the reworded item should enumerate honestly. Rides the Jul 9 script batch — if that lands first, the item shrinks in the same touch (S-3/CL-2 pattern).

### RM-7 — LOW · Cluster (labels, markers, history hygiene)
1. **P1.3 carries no status marker** while its neighbors do (P1.1 ✅, P1.2 ✅, P1.2b ⬜) — yet the rewrite it describes is executed across CONCEPT/VALIDATION (Path One cosmology sections + appendix moves). Mark ✅ or point at the checklist; the asymmetry currently reads as "not started."
2. **Priority row 4** "Strong-field pressure/stress sector (full T_μν) | **Both**" — the "Both" path tag is half-stale post-closure; the item survives as Path One/core.
3. **"A1–A3" (P1.1) and "the A1 conflict" (P1.2)** — post-amendment labeling: P1.1's set is now A1′/A2/A3, and P1.2's "A1 conflict" (the v6.3 shrinking-ruler issue) is a different thing from the A1→A1′ amendment (RHAC-006); one parenthetical each prevents the collision (C-7.4 family).
4. **Version History table stops at v6.2** — no 6.3/6.4 rows; the update sections carry the continuation and CHANGELOG is canonical, but a two-row extension (or one pointer line) is cheap.
5. **Priority row 2** (preferred-frame / simultaneity section) is honestly open and current — optional add per the R-2 formulation: "(receipts in hand: `estif_flow_signature_dynamics.py` 18/18; C-15)".
6. **Preserved-plan scripts, verified against `tests/`:** the v6.1-cited completed scripts all exist (`test_desi_wz_consistency.py`, `derive_mond_from_geometry.py`, `test_sparc_tully_fisher.py`, `test_sparc_bias_analysis.py`, `test_multiplier_derivation.py` — 5/5); of the v6.2 body's planned-future scripts, `test_tully_fisher_correction.py` was in fact written and committed, while `test_cmb_angle_estimate.py`, `test_isw_prediction.py`, `test_omega_tilt_selfconsistent.py` were never written — correctly so (superseded by the fork). History is accurate; no action.
7. **Taboo sweep — clean.** The Mission Statement's core sentence ("3D space is a hypersurface moving through 4D space") is already taboo-free — the only opener in the corpus that never needed the S-5/R-1/C-2 repair. "Flat rotation curves" (7.1) is domain-standard, compliant (extends the R-10.6/CL/V rulings). No grid-ontology "flat," no UKN surface, no shadow test index.

### RM-8 — LINKAGE · Items riding pending actions
- **P2.1's 0.66 text** rides the Jul 9 C4 commit (marginalized rerun exists uncommitted in `audit/Jul 9 Session/`).
- **Header "C-15 (GW sector) DERIVED" and "Fronts 1–3"** — repo receipts are the 15 Jul reconstructions, cross-validated 17 Jul; unconditional wording is gated on the E-2 Mac mini run (same gate as R-11.1, CL-3, V-11).
- **The banner's authoritative pointer** (`PATH_ONE_CHECKLIST.md`) currently carries the stale B-7 ⬜ row (Jul 9 finding); no roadmap text changes when that lands, but the delegation target heals.
- The fronts follow-up items ride the RM-9 decision.

### RM-9 — STRUCTURAL / DECISION · Forward-looking content ends 8 July; the roadmap has no post-v6.4 road
Nothing in the document plans past the v6.3 update: no letter-6.4-revision item, no script-side correction batch beyond the generic errata row, no preferred-frame/Tier-C writing items beyond row 2, no Fronts 1–3 follow-ups (e.g. the black-hole-seed causal-chain writeup the Front 2 result sets up), no RHAC-010 redirect, no honesty-receipt commits. The header Status announces v6.4.0 results the body never absorbs — the same head/body split as VALIDATION's V-1, in planning form. Two coherent branches, mirroring S-9's shape:
- **(a) Pointer branch:** declare the whole file historical — trim the header Status to version + pointer (checklist + RHAC authoritative), restamp per RM-1's restamp branch. Cheap, defensible, consistent with the banner's existing delegation.
- **(b) Content branch (recommended, consistent with the S-1/V-2/C-1 recommendations):** append a "ROADMAP UPDATE — v6.4" section in the document's own idiom — post-null priority table covering the letter 6.4 revision, the script-side batch (C2/C3/C4/C5 + the two honesty receipts), the preferred-frame section, the RHAC-010 redirect of the x_c/√3 items, front follow-ups, and Tier-C docs — then stamp honestly. The roadmap is the natural single home of the next-steps list that currently exists only scattered across seven audit-notes files (§F of AUDIT_NOTES_JUL13 plus the six document passes).
The stamp decision (RM-1) and this content decision are the same fork taken once for four documents.

---

## B. What ROADMAP.md gets right (for the record)

**The banner architecture is the strongest superseded-history mechanism in the corpus.** Two layers, each doing a distinct job: the v6.4.0 banner delegates authority ("checklist wins") and corrects the P items; the v6.3 banner converts the preserved body's claims one by one — EHT/Λ/LISA ✅ → ⚠️ C1-conditional, Ωm = x₀ → consistency relation (C2), the Ω_tilt phases, w = −1.08, and Λ-drift retired by name — and explicitly states what stands (mission, lessons, dark-matter analytics). VALIDATION's banner points at ghosts (V-3); ROADMAP's points at real targets.

**Plan §G is verbatim-executed** — FIND/REPLACE match confirmed unicode-included against the plan text; the only post-plan change is the unrecorded 6.4.1 Version bump (RM-1). The Jul 9 ROADMAP_v6.3_APPEND remains 6/6 integrated (Jul 9 pass) and coexists cleanly with §G — the append is the v6.3 tail, §G the header/banner/footer; no overlap, no conflict.

**The failure record is exemplary in roadmap form.** The v6.1 update preserves "Phase 5.3 ✅ DONE — FAILS" with the full numbers (χ²/N = 10.8; w ≈ −1.08 falsified at 3.5σ; the circularity root cause named) — a roadmap that records its own falsifications in place. The v6.3 fork paragraph preserves the 10.8 → 3.35 → 1.92 descent and names E1/E2 as already-falsified.

**The C4 caveat is wired where it matters most:** P2.1's 0.66 target carries "⚠️ under-marginalized, see C4; re-derive with rd, H₀, Ωm marginalized before committing" — the exact discipline the target-lock receipt encodes, sitting in the one paragraph a Path Two reader would act on.

**Pointers and citations all resolve.** `docs/plan/PATH_ONE_CHECKLIST.md`, `docs/plan/RHAC.md`, `MILESTONE_v6.3_THE_SPLIT.md` (root), `CORRECTIONS_v6.3.1.md` (root), `docs/report/VALIDATION_REPORT.md` Part 2 — all verified this or prior passes. Every script cited as existing exists (5/5 v6.1 citations, this pass, live listing); every script correctly absent is absent. No false citation anywhere in the document.

**Clean surfaces:** no UKN wiring (the disease stays at six documents), no shadow test index (S-9-clean — the tables are plan items, not provenance), taboo-clean including the corpus's only clean opener, and the P1.2 description of the shrinking-ruler retirement + v_flow sideways relabel matches CONCEPT's landed state exactly (the S-7/R-11.3/CL-11.1 chain, closed at doc 5, is consistent here too).

---

## C. Cross-document comparison — the six audited documents vs ROADMAP.md, one by one

| Axis | STATUS | README | CHANGELOG | VALIDATION | CONCEPT | RHAC¹ | **ROADMAP** |
|---|---|---|---|---|---|---|---|
| Stamp honest? | ✗ 6.4.1/12 Jul, no 010 (S-1) | ✓ honest 6.4.1 | ✓ ledger current to 13 Jul | ✗ three-era chimera (V-1/V-2) | ✗ 6.4.1/12 Jul, no 010 (C-1) | ✓ 6.4.1/13 Jul, 010 appended | ✗ 6.4.1/12 Jul, no 010, **Version vs Status self-contradiction** (RM-1) |
| RHAC-010 content | none | ✓ 3 sites | ✓ full ([6.4.1]) | none | none | ✓ appended | none; banner pointer stops at 009 |
| Letter line | "ready" (S-4) | "blocked" (R-2) | n/a | "ready" (V-4) | "deliverable today" (C-5.4) | "ready" (planned header text)² | "ready" ×3 (RM-3) |
| Path Two closure in body | ✓ closed | residue: one clause (R-7) | ✓ [6.4.0] entry | residues: 2.2/2.3 (V-3) | ✓ closed ×3 (one heading, C-5.3) | ✓ RHAC-004 entry | ✗ **live**: bar 10% + full section + row 5 (RM-2) |
| UKN wiring surface | yes (S-2) | no | yes ([6.4.0], CL-1) | no | yes (C-3) | yes (004/005/006) | **no** |
| Frozen-eddy residue (live text) | 3 touches (S-6) | 2 (R-9) | frozen-entry only | 6, incl. header (V-8) | 2 + verb (C-6) | not re-read this pass | 3 (RM-5) |
| Bare 1.92 | ×3 (S-8) | ×3 (R-8) | ✓ attributed at origin | Path One rows (V-5) | 0.66 caveat gap instead (C-5.2) | n/a | ×3 (RM-5) |

¹ RHAC column sourced from the Jul 12/13 session passes and DOC_UPDATE_PLAN §A — no `AUDIT_NOTES_RHAC.md` exists in `audit/`, so RHAC.md has had receipt-wiring and matrix-level checks but no recorded full document pass. Flagged as an audit-trail gap, not acted on.
² Plan §A's landed header text; whether the 13 Jul restamp changed that line was not re-read this pass.

Net: ROADMAP shares the stamp disease with STATUS/VALIDATION/CONCEPT (four documents, one joint decision), shares the letter overclaim with four documents (one formulation, five documents), is **alone** in carrying a live Path Two program, and is **clean** on the two wiring diseases (UKN, shadow index) plus terminology at the opener level.

---

## D. Proposed actions (ROADMAP.md scope) — for your decision, nothing executed

1. **RM-1 + RM-9 (one fork, four documents):** content branch (recommended) — banner pointer → 001…010, one RHAC-010 clause covering the √3/x_c items, append the "ROADMAP UPDATE — v6.4" section, stamp honestly; or pointer/restamp branch — revert both Version fields to 6.4.0 and trim the header Status to a pointer.
2. **RM-2:** closure markers on the three Path Two surfaces (bar, section, priority row 5), citing RHAC-004 + the target-lock receipt's NULL branch; keep P2.1/P2.2 as the historical bar the null was tested against.
3. **RM-3:** the single letter formulation on the three surfaces — same wording as S-4/R-2/V-4/C-5.4, five documents in one touch.
4. **RM-4:** one banner clause mapping Phase 7.3 / Next Steps 5 / the Mac-mini row onto the CONCEPT three-way status (P-law closed; Ωm-derived retired; homogeneous emergence separate and open).
5. **RM-5:** three frozen-eddy touches → "constant cosmic term (imported Λ)"; "derived" → "imported Λ" on the Path One bar; attribution parenthetical at the first 1.92.
6. **RM-6:** reword P1.2b + priority row 1 to the honest split (doc-side applied; script-side C2/C3/C4/C5 pending) — or land the Jul 9 batch first and shrink it in the same touch.
7. **RM-7:** P1.3 ✅; row 4 "Both" → "One"; the two A1-labeling parentheticals; optional Version-History rows; optional row-2 receipts clause.
8. **RM-8:** no text actions; gates as listed (C4 commit, E-2 mini run, checklist B-7).

Per the single-pass preference, all of the above folds into the consolidated execution round from AUDIT_NOTES_JUL13 §F. Sequencing note: the RM-1/RM-9 fork is the same decision as S-1/V-2/C-1 and should be taken **once, before the round**, since it determines which of the four documents receive new content and what the round's honest stamp is.

---

## E. Main-documents pass — closing state

All seven documents read. Standing cross-document batches, final counts:
- **Stamp decision:** 4 documents (STATUS, VALIDATION, CONCEPT, ROADMAP) — one fork, taken once.
- **Letter formulation:** 5 documents (STATUS, README, VALIDATION, CONCEPT, ROADMAP) + the `docs/Letter/6.4/` revision itself.
- **UKN wiring batch:** 6 surfaces + `_index_view` (RHAC-004/005/006, DECLARATION, TEST_INDEX, STATUS table, CHANGELOG [6.4.0], CONCEPT Reframe line) — ROADMAP adds nothing.
- **Path Two closure residues:** README R-7 (one clause), VALIDATION V-3 (two touches or banner reword), CONCEPT C-5.3 (one word), ROADMAP RM-2 (three surfaces — the substantive one).
- **1.92 attribution:** STATUS, README, VALIDATION, ROADMAP (CHANGELOG is the model; CONCEPT's analogue is the 0.66 caveat).
- **Frozen-eddy sweep:** STATUS 3, README 2, VALIDATION 6, CONCEPT 2, ROADMAP 3 (TEST_INDEX task6 wording is the model).
- **Audit-trail gap:** no `AUDIT_NOTES_RHAC.md` in `audit/` despite the pass being marked complete — RHAC.md's receipt-line fixes are fully specified (Jul 12 C-1…C-3) but the full-content read of entries 001–010 vs verdicts, the Scenario H/Q closure-wording checks, and the Summary-Statistics recount check have no written record. Decision item: write the missing notes file, or record that the Jul 12/13 passes are accepted as covering RHAC.

**Notes version:** 1.0 · 19 July 2026
